Use case: background-extraction. Edit this exact sprite atlas: REMOVE THE ENTIRE BROWN/OLIVE BACKGROUND AND FLOOR SHADOWS, replacing every background area with genuine PNG alpha-zero transparency. Preserve every one of the 32 pixel characters exactly, all pixel texture, faces, costumes, poses, dimensions and positions. Do not change the layout or repaint any character. The output MUST be RGBA with genuinely transparent background; NOT black, NOT white, NOT checkerboard, NOT brown. Keep all 8 columns and all 4 rows fully intact. Clean cutout boundaries, no brown glow, no shadow beneath shoes. Only remove the background.
